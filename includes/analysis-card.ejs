```{=html}
<div class="analysis-listing">
  <% for (const item of items) { %>
    <div class="listing-entry" data-categories="<%= item.categories.join(',') %>">
      <div class="analysis-card">
        <div class="analysis-card-content">
          <h2 class="analysis-title"><a href="<%= item.path %>"><%= item.title %></a></h2>
          <% if (item.description) { %>
            <div class="analysis-description"><%= item.description %></div>
          <% } %>
          <div class="analysis-footer">
            <a href="<%= item.path %>" class="read-more">Read more <span class="arrow">→</span></a>
          </div>
        </div>
      </div>
    </div>
  <% } %>
  
```